Value after songScores is always "by" and then the length of the name of the first song


songScoresb (b = array?)

zy9:Dadbattlezh (if song ends in zh it means its the end of the song scores array)

regexr.com/80nv6

if savefile ends in a colon then the thing will just crash

TODO: Add Menu Bar

pip install requests

pip install PyGithub